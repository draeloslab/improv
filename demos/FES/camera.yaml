actors:
  # GUI - TODO: add here a class that will show in a GUI the frame from the camera generated by the Generator

  # CameraReader:
  #   package: actors.camera_reader
  #   class: CameraReader

  # CameraRecorder:
  #   package: actors.camera_recorder
  #   class: CameraRecorder

  Generator:
    package: actors.generator
    class: Generator

  Processor:
    package: actors.processor
    class: Processor

connections:
  # CameraReader.frame_out: [CameraRecorder.frame_in]
  # CameraReader.frame_out: [Processor.frame_in]
  Generator.frame_out: [Processor.frame_in]


redis_config:
  port: 6380