actors:
  # GUI - TODO: add here a class that will show in a GUI the frame from the camera generated by the Generator

  # CameraReader:
  #   package: actors.camera_reader
  #   class: CameraReader

  # CameraRecorder:
  #   package: actors.camera_recorder
  #   class: CameraRecorder

  Generator:
    package: actors.generator
    class: Generator

  Processor:
    package: actors.processor
    class: Processor

connections:

  Generator.q_out: [Processor.q_in, VideoScreen.camera2_in]
  Processor.q_out: [VideoScreen.q_in]
  


redis_config:
  port: 6379